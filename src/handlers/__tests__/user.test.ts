import * as user from '../user';

describe('user handler', () => {
    // broken test
    // it('should create a new user with status 201', async () => {
    //     const req = { body: { username: 'test', password: 'test' } };
    //     const res = {
    //         json({ token }: any) {
    //             // expect(token).toBeTruthy();
    //             expect(1).toBe(2);
    //         }
    //     };
    //     await user.createUser(req, res, jest.fn());
    // });
});
